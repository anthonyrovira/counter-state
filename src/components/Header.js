import React from "react";
import HeaderLogo from "./HeaderLogo";
import HeaderTitle from "./HeaderTitle";

const Header = () => (
  <header>
    <HeaderLogo />
    <HeaderTitle title="React Counter" />
  </header>
);

export default Header;
